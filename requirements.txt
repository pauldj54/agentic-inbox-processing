# Requirements for Agentic Inbox Processing
# Private Equity Email Classification System
# ==========================================

# Azure Identity & Core
azure-identity>=1.15.0
azure-core>=1.28.0

# Azure AI Agent Service (for LLM-powered classification)
azure-ai-projects>=1.0.0
azure-ai-agents>=1.0.0

# Azure Service Bus SDK (message queue for email routing)
azure-servicebus>=7.12.0

# Azure Cosmos DB SDK (email storage and audit logs)
azure-cosmos>=4.7.0
aiohttp>=3.9.0

# Azure Blob Storage SDK (blob upload for link-sourced attachments — see plan.md Dependency gate)
azure-storage-blob>=12.19.0

# Azure Document Intelligence (PDF attachment extraction)
azure-ai-documentintelligence>=1.0.0

# FastAPI Web Framework (dashboard UI)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
gunicorn>=21.0.0
jinja2>=3.1.0

# Environment configuration
python-dotenv>=1.0.0
