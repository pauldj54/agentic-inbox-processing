[project]
name = "agentic-inbox-processing"
version = "0.1.0"
description = "Private Equity Email Classification System"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
